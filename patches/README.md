# Patches

Keep this directory empty unless the upstream Triangle Splatting+ submodule
must be patched. Each patch should include a short note explaining:

- why the patch is required;
- whether it affects baseline behavior;
- how to re-apply or revert it on a fresh clone.
